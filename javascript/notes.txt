let keyword is used to declare variables
let count = 1;
count = 2;      
count = count + 1;


const represents constant, cannot be changed
const PI = 3.14;
PI = 3.14159;   

var is the old way to declare variables in JavaScript
var x = 10;


in js we ccan change the type
let x = 10;        // number
x = "hello";      // string
x = true;         // boolean

console.log() is a JavaScript debugging method used to print output to the browser’s console.
console.log(value);



using backtick `
let name = "Alex";
console.log(`Hello ${name}!`);

alert("Your message here");



let name = prompt("Enter your name:");
console.log(name);


console.error("This is an error message");
if (!username) {
    console.error("Username is missing");
}


if (password.length < 8) {
    console.warn("Password is too short");
}


 
 trim bis used to remove space in first n last 
let name = "   John   ";
console.log(name.trim());

"John"



string.slice(start, end)
string.replace(searchValue, newValue)



arr.push(4);        // add at end
arr.unshift(0);    // add at start



arr.pop();          // remove from end
arr.shift();        // remove from start


🔹 3️⃣ Slice & Splice (IMPORTANT)
slice() → does NOT change original
let a = [1,2,3,4];
a.slice(1,3); // [2,3]

splice() → changes original
let b = [1,2,3,4];
b.splice(1,2,"x","y"); // [2,3] removed
// b → [1,"x","y",4]

arr.includes(2);      // true / false
arr.indexOf(3);       // index or -1
arr.lastIndexOf(2);   // last occurrence


arr.find(x => x > 2);
arr.findIndex(x => x > 2);



🔹 2️⃣ Add / Remove elements (MOST USED 🔥)
➕ Add elements
arr.push(4);        // add at end
arr.unshift(0);    // add at start

➖ Remove elements
arr.pop();          // remove from end
arr.shift();        // remove from start



for(let i = 1; i <= 5; i++){
    console.log(i);
}

let i = 1;

while(i <= 5){
    console.log(i);
    i++;
}

let fruits = ["apple", "banana", "mango"];

for(let fruit of fruits){
    console.log(fruit);
}
